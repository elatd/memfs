{
  "name": "verifs",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -k -n api,web -c cyan,green \"pnpm --filter @verifs/api dev\" \"pnpm --filter @verifs/web dev\"",
    "test": "vitest run",
    "typecheck": "pnpm -r build",
    "build": "pnpm -r build",
    "demo:seed": "tsx scripts/demo-seed.ts",
    "bench:smoke": "tsx benchmarks/src/runners/run-smoke.ts",
    "bench:verifs": "tsx benchmarks/src/runners/run-verifs-regression.ts",
    "benchmark:smoke": "pnpm bench:smoke",
    "benchmark:verifs": "pnpm bench:verifs",
    "benchmark:retrieval": "tsx benchmarks/retrieval/index.ts",
    "mount:smoke:readwrite": "tsx scripts/mount-smoke-readwrite.ts"
  },
  "pnpm": {
    "overrides": {
      "rollup": "npm:@rollup/wasm-node@4.60.2"
    }
  },
  "dependencies": {
    "@verifs/core": "workspace:*"
  },
  "devDependencies": {
    "@verifs/cli": "workspace:*",
    "@types/node": "^22.10.2",
    "concurrently": "^9.1.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
